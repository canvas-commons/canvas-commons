---
'@canvas-commons/2d': minor
---

Flex items retain their minimum content size by default. Use `overflowWrap` of
`'anywhere'` to break long words, or set `minWidth`/`minHeight` to `0` to allow
further shrinking.
