---
'@canvas-commons/webcodecs': minor
---

Add a self-contained in-browser WebCodecs video exporter
(`@canvas-commons/webcodecs`). It encodes video and mixes + encodes audio
entirely in the browser via Mediabunny and the Web Audio API, then writes the
finished mp4 to the output directory.
